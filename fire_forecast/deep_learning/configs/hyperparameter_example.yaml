base_config_path: /home/chlw/software/repositories/fire-forecast/fire_forecast/deep_learning/configs/example0.yaml # path to the base config file to modify with given parameters
output_directory: /data/runs/test # output directory for the training results

training:
    learning_rate: [0.01, 0.001]

model:
    model_args:
        batch_norm: [true, false]
        dropout: [0, 0.05]
        

